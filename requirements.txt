# Runtime dependencies — bundled in the Python Wheel uploaded to S3 artifacts.
# NOTE: delta-spark and pyspark are NOT included here because Glue 4.0 provides them
# natively via --datalake-formats=delta. They are listed in requirements-dev.txt only
# for local test execution.
boto3==1.34.0
python-json-logger==2.0.7
openpyxl==3.1.2
pandas==2.0.3
